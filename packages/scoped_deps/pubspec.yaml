name: scoped_deps
description: A simple dependency injection library built on Zones
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  meta: ^1.0.0

dev_dependencies:
  mocktail: ^1.0.0
  test: ^1.0.0
  very_good_analysis: ^5.1.0
