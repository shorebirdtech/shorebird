# Server Configuration
PORT=8080
HOST=0.0.0.0

# JWT Secret for authentication (REQUIRED - generate a secure random string)
# Generate with: openssl rand -hex 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Default admin user (created on first start if no users exist)
ADMIN_EMAIL=admin@localhost
ADMIN_PASSWORD=admin123

# S3 Storage Configuration (REQUIRED)
S3_ENDPOINT=localhost
S3_PORT=9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_USE_SSL=false
S3_REGION=us-east-1
S3_BUCKET_RELEASES=shorebird-releases
S3_BUCKET_PATCHES=shorebird-patches

# Optional: Database Configuration
# Currently uses JSON file storage. For production, implement PostgreSQL.
# DATABASE_URL=postgresql://localhost:5432/shorebird
