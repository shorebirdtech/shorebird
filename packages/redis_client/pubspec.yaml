name: redis_client
description: A Dart library for interacting with Redis.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  resp_client: ^1.2.0

dev_dependencies:
  test: ^1.19.2
  very_good_analysis: ^5.1.0
